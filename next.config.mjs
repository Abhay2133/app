/** @type {import('next').NextConfig} */
const nextConfig = {
    // output:"export",
    // experimental : {
    //     outputFileTracingExcludes : {
    //         "/api/pipe" : ['route.ts']
    //     }
    // }
};

export default nextConfig;
